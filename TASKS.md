# 残作業

作業前に `AGENTS.md` を読むこと。

タスクは上から順に、着手しやすい順。

---

## T-1. 実機・エミュレータでの表示確認  【最優先・未実施】

**状態**: 未着手。ロジックのテストは通っているが、**見た目を一度も確認していない**。

前任者の作業環境にエミュレータが無く、ビルドとユニットテストまでしか
検証できていない。以下は「コードとしては正しいが、画面で見ていない」状態。

Android Studio で起動して確認し、崩れていれば直すこと。

### 確認項目

- [ ] ホーム画面で、問題集が1個しかないタブ（積分法・芳香族1 など8タブ）を開く
      → **ボタンが1個だけ表示され、残り9個は見えない**こと。
      仮の文字（"22" "33" など）や、別のタブのタイトルが残っていないこと
- [ ] タブを左右に何度かスワイプしてから同じタブに戻る
      → ボタンの内容が入れ替わっていないこと
- [ ] 検索画面を開く → 何も入力していない状態で**検索履歴が出る**こと
      （初回は空。何度か検索してから再度開く）
- [ ] 検索履歴の行をタップ → その語で**検索し直される**こと
- [ ] 問題集を解いて結果画面へ → **グラフが表示される**こと
- [ ] 別の問題集を10回以上解いてから、最初の問題集の結果画面を開く
      → **グラフのデータが消えていない**こと
- [ ] 「複素数平面」タブの問題を解く → **日本語の問題文が表示される**こと
      （修正前は何も表示されなかった）
- [ ] 数式を含む選択肢がある問題（複素数の演算・ベクトル・極限と三角関数・
      特殊な置換積分）→ **数式がボタン内に収まって読める**こと
- [ ] 全問解き終えたとき **「終わり！」が表示される**こと
      （修正前は表示されなかった）

崩れていた場合の対処案:

- 余ったボタンの扱いは `MyViewHolder#bind` の `else` 節。現在は `INVISIBLE`。
  空白が不自然なら `GONE` に変える（ただし TableRow 内で片方だけ `GONE` にすると
  残った方が広がるので、実機で見て判断すること）
- 数式がボタンからはみ出す／小さすぎる場合は `QuizQuestionActivity#showChoiceText` の
  `.textSize(button.getTextSize())` を調整する
- 問題文の表示先の切り替えは `QuizQuestionActivity#showQuestionText`。
  レイアウトは `activity_quizquestion.xml` の `questionTextView`（通常テキスト）と
  `questionMathView`（数式）

---

## T-2. 未記入の設問を埋める  【所有者の入力待ち】

**状態**: 保留。**内容を創作しないこと。**

`app/src/main/assets/initial_data_math.json` の「油脂・セッケンのまとめ2」に、
問題文が空の設問が2問ある。選択肢は `["1","2","3","4"]` というプレースホルダ。

```json
{ "question": "", "choices": ["1", "2", "3", "4"], "correct_answer": 0 }
```

現在は `DatabaseHelper#isUsable` が false を返し、出題・検索の対象から外れる。
この問題集は出題対象が0問になり、開くとメッセージを表示して前の画面に戻る。

**所有者から問題文をもらったときだけ**データを埋める。埋めたら次のテストの
期待値を更新すること。

- `QuizDataParsingTest#未記入の設問は出題対象から外れる`（`51 - 2` の部分）
- `DatabaseHelperTest#全件が登録されている`（同上）
- `LatexRenderingTest#同梱データをすべて表示できる`（`49 * 5` の部分）

学習用アプリの問題文を第三者が推測で作ると、誤った内容を教材に混ぜることになる。
必ず所有者に確認すること。

---

## T-3. レイアウトXMLの点検  【未実施】

**状態**: 未着手。Java 側12クラスは点検済みだが、`res/layout/*.xml` は未点検。

見るべき観点:

- ハードコードされた文字列（`android:text="TextView"` などの仮の値が残っていないか）
- `dp` と `sp` の使い分け（文字サイズは `sp`）
- 画面幅が狭いときにボタンや文字がはみ出さないか
- `contentDescription` が無い `ImageButton`（`settingButton` `searchButton` など）

対象ファイル:

```
res/layout/activity_main.xml
res/layout/activity_pager2.xml
res/layout/activity_quizquestion.xml
res/layout/activity_search.xml
res/layout/quiz_results_activity.xml
res/layout/search_result_item.xml
```

---

## T-4. 設定画面  【未実装】

`MyViewHolder` の歯車ボタンは、押すとトーストを出すだけ。

```java
settingButton.setOnClickListener(v -> {
    Toast.makeText(this.getContext(),"設定画面への遷移。ここまではOKです。", ...);
    // 追加機能を実装する予定
});
```

何を設定させたいかが決まっていないため未実装。仕様を決めるところから必要なので、
**所有者に確認してから着手すること。**

---

## 完了済み（参考・再着手不要）

2026年9月に修正した不具合。詳細は `README.md` の「未完成・既知の問題」を参照。

1. **検索が常に0件を返していた** — 初期データの投入が JSON の形式を取り違えて
   失敗し、例外がログ出力だけで握りつぶされ、問題テーブルが空のまま作られていた
2. `onUpgrade()` がテーブルを DROP した後に再作成していなかった
3. 問題文が未記入の設問がそのまま出題対象になっていた
4. **他の問題集を10回解くと成績グラフが空になっていた** — 保持件数の管理が
   問題集ごとではなくテーブル全体を対象にしていた
5. **余ったボタンに前のタブの内容が残り、無関係な問題集が開けた**
6. 検索履歴が宣言だけの死んだコードだったのを実装した
7. **日本語の問題文が画面に表示されなかった** — JLaTeXMath は日本語のグリフを
   持たないのに、全問題文をそこに流していた
8. 選択肢ボタンが数式表示に未対応だった

いずれも「修正前のコードでテストが失敗すること」を確認したうえで直してある。
